<?php

/**
 * AI 토큰 단가표 — 백만 토큰당 미국 달러.
 *
 * 여기 값은 요금 청구가 아니라 <b>어디에 돈이 쓰이는지 보기 위한 어림값</b>이다.
 * 공급사 단가는 자주 바뀌므로 코드에 박지 않고 이 파일에 모아 둔다. 값을 고치면
 * 그 뒤의 호출부터 새 단가로 계산되고, 이미 기록된 금액은 그때의 값으로 남는다.
 *
 * 모델 이름이 표에 없으면 그 엔진의 default 를 쓴다.
 */
return [
    'gemini' => [
        'default' => ['in' => 1.25, 'out' => 10.00],
        'gemini-2.5-flash' => ['in' => 0.30, 'out' => 2.50],
    ],

    'claude' => [
        'default' => ['in' => 3.00, 'out' => 15.00],
    ],

    'openai' => [
        'default' => ['in' => 2.50, 'out' => 10.00],
    ],
];
