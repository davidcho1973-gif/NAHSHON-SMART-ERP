<?php

/*
|--------------------------------------------------------------------------
| 이 배포는 누구의 것인가
|--------------------------------------------------------------------------
|
| 코드는 하나, 배포는 고객마다 하나 — 그 방식이 성립하려면 "고객마다 다른 값"이
| 전부 한 군데 모여 있어야 한다. 값이 코드에 흩어져 있으면 새 고객마다 코드를
| 고치게 되고, 그 순간 고객 수만큼 서로 다른 코드가 생긴다. 1년 뒤에는 한 곳을
| 고쳐서 전부에 반영할 방법이 없어진다.
|
| 그래서 규칙은 하나다 — 고객 이름도, 고객만의 숫자도, 코드에 넣지 않는다.
| 새 고객은 이 파일이 읽는 환경변수만 채우면 선다.
|
| 여기(설정 파일)와 org_settings 표(App\Support\Org)의 역할이 다르다.
|
|   이 파일 — 배포할 때 정하고 거의 안 바뀌는 값. 스케줄 시각처럼 매분 돌아가는
|             코드가 읽는 값은 반드시 여기 있어야 한다. 설정 파일은 캐시되므로
|             데이터베이스를 깨우지 않는다.
|   표      — 고객사 관리자가 화면에서 직접 고치는 값(회사 이름·색·연락처).
|             여기 값은 그 표의 기본값이 된다.
|
*/

return [

    // ── 신원 ────────────────────────────────────────────────────────────
    // 화면·문서·이메일·앱 이름에 나가는 이름. 고객사 이름이다.
    'name' => env('ORG_NAME', 'DASOL PRISM'),

    // 이 배포가 아직 아무것도 설정하지 않은 채로 서 있는가. 새 고객 배포가 기본값
    // 그대로면 화면·이메일·인쇄물에 남의 회사 이름이 나가는데, 화면은 멀쩡해
    // 보여서 고객이 먼저 발견한다. /build-version 과 org:provision 이 이 값을 본다.
    'configured' => env('ORG_NAME') !== null,

    // 좁은 자리(휴대폰 상단, 홈 화면 아이콘 밑)에 쓰는 짧은 이름.
    // 기본값을 두지 않는다. 여기서 ORG_NAME 을 붙잡으면 화면에서 회사 이름을 바꿔도
    // 홈 화면 아이콘 이름만 옛 이름으로 남는다 — 가장 눈에 안 띄는 자리라 오래 간다.
    'short_name' => env('ORG_SHORT_NAME'),

    // 계약서·W-9 같은 법적 서류에 찍히는 법인명. 상호와 다를 수 있다.
    'legal_name' => env('ORG_LEGAL_NAME'),

    // 이 배포의 첫 최고 관리자.
    //
    // 예전에는 마이그레이션이 특정 개인을 최고 관리자로 만들어 두었다. 배포 하나일
    // 때는 편했지만, 고객마다 배포하는 지금은 고객 데이터베이스마다 우리 계정이
    // 들어간다는 뜻이었다 — 고객이 지워도 다음 배포에 되살아났다.
    //
    // 이제 첫 관리자는 배포할 때 정한다. 여기에 넣어 두면 org:provision 이
    // --admin 없이도 그 사람을 최고 관리자로 세운다.
    'admin_email' => env('ORG_ADMIN_EMAIL'),

    // companies 표에서 자사(자기 회사)를 가리키는 코드. 시드와 마이그레이션이 쓴다.
    'code' => env('ORG_CODE', 'DASOL-PRISM'),

    // ── 겉모습 ──────────────────────────────────────────────────────────
    'color' => env('ORG_COLOR', '#0ea5e9'),

    // 사람이 연락할 곳. 작업자 화면에서 "안 되면 여기로" 로 쓴다.
    'support_email' => env('ORG_SUPPORT_EMAIL'),
    'support_phone' => env('ORG_SUPPORT_PHONE'),

    // ── 근무 규칙 ───────────────────────────────────────────────────────
    // 회사마다 다르다. 상수로 박아 두면 두 번째 고객에게서 바로 막힌다.
    'attendance' => [
        // 저녁 마감 — 그날 일이 끝난 사람을 그날 안에 마감한다.
        'evening_finalize_at' => env('ORG_EVENING_FINALIZE_AT', '20:00'),

        // 아직 현장에 있는 사람은 건너뛴다. 저녁 마감이 연장 근무를 자르면 안 된다.
        'evening_grace_minutes' => (int) env('ORG_EVENING_GRACE_MINUTES', 30),

        // 안전망 — 저녁에 건너뛴 사람을 어제 날짜로 정리한다.
        'safety_net_at' => env('ORG_SAFETY_NET_AT', '00:05'),

        // 간접고용(협력사) 자동 퇴근 시각. 현장 로컬 기준 24시간제.
        'indirect_cutoff_hour' => (int) env('ORG_INDIRECT_CUTOFF_HOUR', 16),

        // 같은 사람이 이 시간 안에 또 찍으면 중복으로 본다.
        'duplicate_window_minutes' => (int) env('ORG_DUPLICATE_WINDOW_MINUTES', 5),
    ],

    // ── 현장 경계(GPS) ──────────────────────────────────────────────────
    // 현장 크기와 건물 구조에 따라 다르다. 좁은 현장에 넓은 값을 쓰면
    // 길 건너에서 찍히고, 반대면 현장 안에서 못 찍는다.
    'geo' => [
        'exit_buffer_meters' => (int) env('ORG_GEO_EXIT_BUFFER_METERS', 80),
        'dwell_seconds' => (int) env('ORG_GEO_DWELL_SECONDS', 600),
        'min_accuracy_meters' => (int) env('ORG_GEO_MIN_ACCURACY_METERS', 75),
    ],

    // ── 급여 ────────────────────────────────────────────────────────────
    // 세율(FICA·Medicare)은 여기 없다. 그건 회사 정책이 아니라 연방법이고,
    // 고객이 고칠 수 있게 두면 잘못 고쳤을 때 세금 신고가 틀어진다.
    'payroll' => [
        'standard_hours_per_week' => (int) env('ORG_STANDARD_HOURS_PER_WEEK', 40),
        'period_days' => (int) env('ORG_PAY_PERIOD_DAYS', 14),
    ],

    // ── 자동 작업 시각 ──────────────────────────────────────────────────
    // routes/console.php 가 읽는다. 매분 도는 자리라 데이터베이스를 볼 수 없다.
    'schedule' => [
        'docs_expiry_alert_at' => env('ORG_DOCS_ALERT_AT', '07:00'),
        'ops_digest_at' => env('ORG_OPS_DIGEST_AT', '18:00'),
        'rental_accrual_at' => env('ORG_ACCRUAL_AT', '05:30'),
    ],

];
